{
  "name": "@chaves/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "npm run build -w @chaves/domain && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "npm run build -w @chaves/domain && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@supabase/supabase-js": "^2.47.10",
    "fastify": "^5.2.0",
    "pdf-lib": "^1.17.1",
    "zod": "^3.24.1",
    "@chaves/domain": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
